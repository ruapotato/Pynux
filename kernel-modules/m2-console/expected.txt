# M2.4/M2.5: the [P] marker prepended by hamnix_console_write proves the
# Hamnix-registered console handled each printk. /proc/consoles must list
# `hamnix0` (E=enabled), and the kernel logs the console becoming active.
[M2-CONSOLE] registered
[P]
printk: legacy console [hamnix0] enabled
hamnix0
[M2-CONSOLE] unregistered
