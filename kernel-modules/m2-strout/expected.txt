# M2.3: the banner is written byte-by-byte via the Hamnix-built driver
# loop (LSR poll + outb), reading bytes through IndexExpr on a Ptr[char].
[M2-STROUT] hello from a hamnix driver
[M2-STROUT] bye
